# Cornix BASE 키맵 업데이트 v1.6.1

STENO 배열, 61개 기본 약어, 12개 퀵 약어, LED·RF 설정은 변경하지 않고 BASE 레이어의 세 위치만 수정한다.

| 물리 위치 | 짧은 탭 / 기본 | 홀드 |
|---|---|---|
| 왼쪽 하단 `[` 위치 | `[` | `Ctrl+Y` |
| 오른쪽 하단 `]` 위치 | `]` | `RCtrl` |
| 오른쪽 최상단 | `Ctrl+A` | 없음 |

실제 바인딩:

```dts
&ht200 LC(Y) LBKT
&ht200 RCTRL RBKT
&kp LC(A)
```

두 bracket 키의 판정 시간은 기존 `ht200` 정의에 따라 200ms다.

- `[` 키를 짧게 누르고 놓으면 `[`
- `[` 키를 홀드하면 `Ctrl+Y`
- `]` 키를 짧게 누르고 놓으면 `]`
- `]` 키를 홀드하거나 다른 키와 조합하면 `RCtrl`
- 오른쪽 최상단 키는 누를 때마다 `Ctrl+A`를 한 번 전송
