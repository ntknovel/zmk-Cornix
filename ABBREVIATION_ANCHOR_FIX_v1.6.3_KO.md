# Cornix STENO v1.6.3 — 약어 앵커 수정

## 실기에서 발생한 증상

- `약어 + 겹모`의 작은따옴표 같은 단순 2키 exact chord는 정상
- 완성 약어 또는 퀵 약어는 약어키를 홀드하고 나머지 키를 탭하면 무출력
- 경우에 따라 약어키 단독 기능인 `!` 또는 `"`만 출력

## 원인

Cornix 엔진에는 40ms chord-settle, 50ms key-up correction, 120ms late-tap 판정이 있다. 첫 키 이후 늦게 들어온 역할을 짧게 탭하고 다른 키를 계속 홀드하면 그 역할을 오타 후보로 제거한다. 약어키를 modifier처럼 홀드하는 자연스러운 입력에서는 자음·모음 탭이 모두 이 조건에 걸릴 수 있었다.

## 수정

- `약어L/약어R`이 포함된 스트로크는 약어 앵커로 처리
- 모음이 없고 자음 역할이 있는 `겹L/겹R` 2차 사전 스트로크도 앵커로 처리
- 앵커가 눌린 동안 먼저 해제된 역할을 exact mask에서 제거하지 않음
- 앵커 해제 시 한 번만 완성 약어 또는 quick macro를 조회
- extra role이 있으면 exact lookup이 실패하여 무출력
- 일반 음절의 50ms 오타 복구는 유지

## 회귀 시험

- 약어L 홀드 + 순차 자음 탭: `그런`
- 약어R 미러 입력: `그런`
- 겹L 홀드 + 순차 자음 탭: `그럼`
- 약어L 홀드 + ㅡ 탭: quick M0 한 번
- 앵커 없는 일반 음절 오입력 복구 유지

`python3 tools/validate_package.py .`와 `bash validation/run_host_tests.sh`가 모두 이 상태 테스트를 실행한다.
